# Regole di Sviluppo & Git Automation

- **Expo Documentation**: Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
- **Auto Git Commit & Push**: Al completamento di ogni nuova feature, bugfix o modifica richiesta, esegui sempre di default un `git commit` chiaro e descrittivo e un `git push origin main` sul repository GitHub (`https://github.com/federicocolombo12/EloPingPong`).
